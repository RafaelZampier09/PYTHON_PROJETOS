saldo = 1000
limite = 200

print(saldo is limite) # Verifica se as variaveis ocupam o mesmo espaço de memoria
print(saldo is not limite) # Verifica se as variaveis NÃO ocupam o mesmo espaço de memoria